import 'package:flutter/material.dart';

class ColorManager {
  static const Color primary = Color.fromARGB(255, 0, 0, 0);
  static const Color sub = Colors.teal;
  static const Color whats = Color.fromARGB(255, 7, 94, 84);
    static const Color whats2 = Color.fromARGB(255, 18, 140, 126);
  static const Color text = Colors.white;
  static const Color message = Color.fromARGB(255, 0, 30, 75);
  static const Color error = Colors.red;
  static const Color success = Colors.grey;
}
